openapi: 3.0.0
info:
  title: Purchase and Authentication API
  description: API for managing purchases and authentication
  version: 1.0.0

servers:
  - url: http://localhost:8080/api/v1

paths:
  /auth/login:
    post:
      summary: Authenticate user
      tags:
        - Authentication
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AuthenticationRequest'
      responses:
        '200':
          description: Successful authentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthenticationResponse'
        '401':
          description: Unauthorized
  /purchase/file:
    post:
      summary: Upload purchase file
      tags:
        - PurchaseFile
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '201':
          description: File uploaded successfully
    get:
      summary: Get purchases files summarized
      tags:
        - PurchaseFile
      parameters:
        - name: fileName
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PurchaseFileResponse'
  /purchase:
    get:
      summary: Get purchases records details by file
      tags:
        - Purchase
      parameters:
        - name: fileName
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PurchaseResponse'

components:
  schemas:
    AuthenticationRequest:
      type: object
      properties:
        username:
          type: string
        password:
          type: string

    AuthenticationResponse:
      type: object
      properties:
        jwt:
          type: string

    PurchaseResponse:
      type: object
      properties:
        purchaserName:
          type: string
        itemDescription:
          type: string
        itemPrice:
          type: decimal
        purchaseCount:
          type: integer
        merchantAddress:
          type: string
        merchantName:
          type: number
        inclusionDate:
          type: string
          format: date-time

    PurchaseFileResponse:
      type: object
      properties:
        fileName:
          type: string
        inclusionDate:
          type: string
          format: date-time
        gross:
          type: decimal